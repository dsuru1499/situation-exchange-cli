import { DataTablesDirective } from './datatables.directive';

describe('DatatableDirective', () => {
  it('should create an instance', () => {
    // const directive = new DataTableDirective();
    // expect(directive).toBeTruthy();
  });
});
